node_modules
dist
.ssr

# NOTE: video-src/ (the ~30 MB full-size hero masters) is deliberately NOT
# ignored — the originals are tracked so they survive a fresh clone.
# public/*.mp4 are the compressed derivatives, regenerated by
# `npm run compress:video`.
*.local
.DS_Store
